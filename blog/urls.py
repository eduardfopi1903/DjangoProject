from django.conf.urls import url
from . import views
urlpatterns = [
   # url(r'^(?P<slug>[])$', blog.post_detail, name="blog_post_detail"),
   # url(r'^$', views.post_list, name="blog_post_list" ),
]

